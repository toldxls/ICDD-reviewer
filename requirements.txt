# Python dependencies for the PXRD review tool.
#   pip3 install -r requirements.txt

PyMuPDF>=1.24.3      # PDF text extraction + page render (pxrd_review/cell_lambda_check.py, gui/). >=1.24.3
                     # is where the module became importable as 'pymupdf'; the old 'fitz' alias is deprecated
python-docx>=1.2     # docx parsing + Word comments API (pxrd_review/annotate_review.py)
                     # >=1.2 is REQUIRED: Document.add_comment() landed in 1.2.0. With 1.1.x installed
                     # every flagged entry raises AttributeError, is caught per-entry, and the run
                     # "succeeds" having written no findings at all — a silent no-op.
lxml                 # docx XML via lxml.etree (pxrd_review/annotate_review.py); also a python-docx dependency
certifi              # CA certs for HTTPS to the Mindat API (pxrd_review/mindat.py); a dependency in pyproject.toml, and used if importable
Flask                # the review-mode GUI (pxrd_review/gui/review_gui.py); a dependency in pyproject.toml, though the CLI checks run without it
openpyxl>=3.1   # xlsx in/out (EPMA, Gladstone-Dale, PXRD tables)
