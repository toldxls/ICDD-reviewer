# Python dependencies for the PXRD review tool.
#   pip3 install -r requirements.txt

PyMuPDF              # imported as 'fitz' — PDF text extraction (tools/cell_lambda_check.py)
python-docx>=1.1     # docx parsing + Word comments API (tools/annotate_review.py); >=1.1 for the comment API
lxml                 # docx XML via lxml.etree (tools/annotate_review.py); also a python-docx dependency
certifi              # optional — used for CA certs if importable, for HTTPS to the Mindat API (mindat/mindat.py)
Flask                # optional — the review-mode GUI only (gui/review_gui.py); not needed for the CLI checks
