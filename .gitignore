.mindat_key
.cache/
# local analysis scratch (mined DC fields etc. — derived from the private corpus)
.scratch/
__pycache__/
*.pyc
.DS_Store
# packaging / editable-install artifacts
*.egg-info/
build/
dist/
# review-mode GUI sidecars (written into a data folder's review_out/, never the repo)
gui_cache.json
triage.json
triage_report.txt
# local-only working brief for Claude Code; not shared in the repo
CLAUDE.md
